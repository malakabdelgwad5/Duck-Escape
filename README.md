# Hex-Duck-Escape
A simple strategy game where the player tries to trap an AI-controlled duck inside a hexagon grid. The duck attempts to reach the edge, while the player blocks its path. The player wins if the duck is fully trapped; otherwise, the duck escapes. Includes clear tiles and multiple difficulty levels."
